import './Events.css';
import { Button } from '@mantine/core';

function Events() {
    return (
        <div className="Events">
            <Button>Hello</Button>
        </div>
    );
}

export default Events;
